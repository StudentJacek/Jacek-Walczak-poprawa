/**
 * Oblicza całkę nieoznaczoną funkcji f(x).
 * @param f funkcja, której całka ma być obliczona
 * @param x zmienna całkowania
 * @return funkcja pierwotna f(x)
 */
double integral(double (*f)(double x), double x) {
    // zwraca wartość całki nieoznaczonej funkcji f(x)
    return integral of f(x) dx;
}